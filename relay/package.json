{
  "name": "holler-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Reference relay for the Holler push-to-talk wire protocol (Cloudflare Worker + Durable Object).",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types --strict-vars=false",
    "test": "vitest run",
    "typecheck": "npm run types && tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.125.0"
  }
}
