# Handoff - 2026-08-21

## Current State
- Branch: `main` @ e957ad4 (`fix(core): liveness window bound without integer overflow`). This handoff lives on branch `docs/handoff-2026-08-21` (pushed, no PR — open one after the local review gate).
- Open PRs: **#24** `chore(health): per-component health script + 2026-08-21 baseline (#13)` — all checks green on its last run; all Codex threads resolved; watcher STOPPED on Mick's request (pause). It needs one `gh pr update-branch 24 --rebase` (behind `main` after #27) and then merge when CLEAN. Do NOT push further changes to it before running the local review team (see Next).
- Merged today: #22 (verify.sh/lint), #23 (merge policy), #28 (UDID sim destinations), #29 (connected-after-handshake, closes #21), #27 (liveness watchdog, closes #5), Dependabot #16 #17 #18 #20 #19 (relay on TypeScript 7.0.2).
- Uncommitted changes: none.
- Background monitors: none (all stopped).

## What Was Done (this session, 2026-08-20 → 21)
- Forged and activated the Dollhouse ensemble `apple-product-team` (run r-2026-08-20-apple1): personas architect/CI-release/issue-spec-PO; skills swift-component-design, github-issue-feature-spec, apple-ci-pipeline-setup, component-health-verification; agents apple-feature-implementer (merges when gate green), component-health-verifier (read-only); templates; memories apple-platform-facts (dated), apple-product-team-runbook (lessons appended), holler-project-context.
- Bootstrapped github.com/mickdarling/holler (AGPL-3.0): Swift 6.2 SwiftPM layers (Core/Transport/Audio/PTT/Features + TestSupport), XcodeGen apps (iOS/watchOS/macOS), Cloudflare DO relay (37 tests, TS 7), CI (verify/simulators/codeql/relay), branch ruleset (PR + required checks + thread resolution, unattributed-approval OFF), 16 spec issues + health issues #25/#26, labels, templates, ADR-0001..0004, docs.
- `scripts/verify.sh` (single verification entry point; sim lane resolves simulator UDIDs; `sim <scheme>`), `scripts/health.sh` (per-component honest report; in PR #24), `scripts/check-boundaries.sh`, `scripts/check-size.sh`.
- 77 Swift Testing tests on `main`; health baseline: GREEN 8 / YELLOW 2 (HollerAudio, HollerPTT lack test targets) / RED 0; CodeQL 0 alerts.

## Update (same day, later session)
- The `production-code-review-team` ensemble is now in this Mac's Dollhouse portfolio (imported via Dollhouse from the VM; 8/8 elements activate). The gate is encoded in `apple-product-team-runbook` (entry tagged `review-gate`), `apple-feature-implementer` (STEP 4.5/5.5), and `CONTRIBUTING.md` / `CLAUDE.md` (this branch).
- #24 went through the gate: pre-change review confirmed both open Codex P2s and found three more; fixed in 54457ec, baseline regenerated in 8c0d1ec; review record is a PR comment. Merge once CI is green.

## What Needs to Happen Next
1. ~~New process: import/rebuild the local review ensemble and encode it~~ **DONE (see Update above):** `production-code-review-team` is in the portfolio and the gate is encoded in the runbook, the implementer agent, and CONTRIBUTING.md/CLAUDE.md. The element export lives in `docs/agent/dollhouse/production-code-review-team/` for fresh clones.
2. Finish #24 through that gate: local review of the PR diff → fix if needed → `gh pr update-branch 24 --rebase` → wait green → merge (rebase). Then close #13 (auto) and re-run `scripts/health.sh` on `main` if anything changed.
3. Next implementable specs (lowest unblocked first): #2 iOS shell (PushToTalkChannelControlling seam + HollerPTTTests → also closes #26), #25 HollerAudio tests, #10 settings, #8 audio-session interruptions, #3/#4 Watch/Mac manual notes. #12 spike, #6 APNs, #11 signing need Mick's inputs.
4. Mick-only items still open: relay hosting decision (deploy `relay/` to Cloudflare → set `HOLLER_RELAY_URL` in project.yml), signing secrets (#11), APNs .p8 (#6).

## Active Context
- Dollhouse: activate `apple-product-team` + `holler-project-context` before working. Runbook has two "Lessons" entries (PR #22; PRs #24/#27/#29) — read them.
- Local toolchain: Xcode 26.3 usable; `xcode-select` still points at CLT so scripts export `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` when needed. Brew: xcodegen 2.46.0, swiftlint 0.65.0, periphery 3.8.0, xcbeautify 3.2.1.
- Merge mechanics: strict up-to-date checks + slow macOS runner queue ⇒ run ONE serialized merge watcher; required-checks-green + `BLOCKED` ⇒ unresolved review thread (Codex auto-reviews every push; address each thread with a reply stating what changed, then resolve). `UNKNOWN` right after a merge is transient.

## Gotchas
- Codex caught ~20 real findings across #24/#27/#29 that self-review missed (concurrency after `await`, nonce resets, handshake/stop race, report cells rendered ✅ without the check running). The local review team is meant to catch these before CI.
- `scripts/health.sh` runs the full simulator lane (~5 min); `--no-sim` marks app rows ❓.
- `swift test --filter "<Suite display name>"` does not match; filter by target/type name.
- macOS `/bin/bash` is 3.2 (guard optional arrays under `set -u`); BSD sed has no `\w`; zsh uses `$pipestatus`.
- Don't chain `&& git push` after a grep-filtered verify pipeline — gate on `scripts/verify.sh all`'s own exit code.
- A watcher may rebase a remote branch while you commit locally: `git pull --rebase` before push.
