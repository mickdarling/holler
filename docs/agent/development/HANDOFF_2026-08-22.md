# Holler — handoff 2026-08-22

Written at the end of a long session so the next one starts with the facts. Read `CLAUDE.md` and `CONTRIBUTING.md` first (binding). Activate the Dollhouse ensemble `apple-product-team` and the memory `holler-project-context` (it has dated entries for this session, including the stop point and the round-21 finding).

## Direction from Mick (this session, binding)

- Goal now: a **minimal project that does what the spec says**, on real devices. No more refactoring or hardening of the PushToTalk gate beyond what device testing shows is needed.
- The review gate in CLAUDE.md still applies (review the diff before touching a PR and again before commit), but run it once per change and fix real defects — do not spawn open-ended adversarial rounds. The gate went through 21 of them this session; that was more than this stage needed.
- Codex auto-review posts a P2 stream on `scripts/health.sh` that does not end. Honest `❓` beats a guessed `✅`; defer edge cases to a tracking issue rather than chasing them indefinitely.

## Where things are (2026-08-22, ~09:45 EDT)

**main** = `7e03078`.

**PR #33 — iOS shell + PushToTalk channel seam (closes #26; most of #2)** — branch `feature/2-ios-shell`, head `c57f8aa`, worktree **`~/Developer/holler-wt2`** (not the main checkout).
- Last push: quick-tap fix (a release before the begin callback is remembered; the begin stops the system instead of pressing the coordinator) + the round-21 predicate fix (a retired join accepted while only an *older* leave is pending is left again). 98 HollerPTT tests / 15 suites; local gate green (verify all + 2× simulator). The Codex P1 thread for the quick-tap was answered and resolved.
- At handoff time CI for `c57f8aa` was running; a background job was waiting on it and would then run the merge helper. **Check `gh pr checks 33`.** If all checks pass, zero alerts, zero unresolved threads: merge (rebase) with `scratchpad/merge-pr.sh 33` equivalent — `gh pr merge 33 --rebase --delete-branch` — then `git worktree remove ~/Developer/holler-wt2`.
- If Codex posted new threads on `c57f8aa`: answer them; fix only real defects; for edge cases reply "tracked" and file one issue.
- Known gaps are in ADR-0005 and filed as #34–#38. **#38** = PTT membership is not surfaced in the UI and there is no reconnect path after a terminal leave (the talk button is silently inert until the next `start()`); this matters for device testing UX.

**PR #24 — component health baseline (#13)** — branch `chore/13-health-baseline`, head `068ee27`.
- CI green, zero alerts. Baseline `docs/health/2026-08-21.md` = GREEN 8 · YELLOW 2 · RED 0 over 10 rows (regenerated as the last commit of every push).
- **Two Codex threads open:** `scripts/verify.sh:51` (the `|| cat` fallback swallows an xcbeautify failure — propagate it) and `scripts/health.sh:449` (the *test* size check should use the test targets' compiled-source manifests, as the production checks do). Fix or defer-with-issue, answer, resolve, merge.
- Local pipeline pattern used all session (scratchpad): patch → `bash -n` + `shellcheck -S warning` → self-test → full `scripts/health.sh` run → guard (totals unchanged, 10 rows, **no `health.sh: line` on stderr**) → commit → regenerate baseline as a second commit → push → reply + resolve → wait CI. The stderr guard exists because one pushed commit had an unbound-variable bug the totals guard missed.
- Thread queries must **paginate**: the PR has 100+ threads and a `first:100` query hid three unresolved ones for a while.

## Path to devices (in order)

1. Merge #33 and #24 as above. Stop the gate review loop.
2. **Signing + entitlements (#11).** PushToTalk does not run in the simulator. Needs the Apple Developer team in `project.yml`, bundle IDs, `com.apple.developer.push-to-talk`, background modes `push-to-talk` + `audio`, an APNs key; then a TestFlight or direct install on two iPhones. This is the actual blocker.
3. **Deploy the relay.** The app's relay URL is a placeholder; `relay/` was never deployed (Cloudflare — needs Mick's auth; ask before deploying). A baked URL is fine for the first test; settings (#10) are not required for it.
4. **APNs push restore (#6)** for background/killed-app receive. Skippable for a first foreground two-phone test; required for "always-on".

Not needed before the first device test: #25 (HollerAudio tests), #10 (settings), #8 (call interruptions), Watch/Mac shells.

## Tooling facts

- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` for everything (`xcode-select` still points at CLT). `scripts/verify.sh all` and `scripts/verify.sh sim Holler-iOS` pass locally.
- SwiftLint strict: file ≤200 lines, type body ≤150, line ≤120, no trailing newline/comma; `swiftlint lint --strict --quiet Sources/HollerPTT Tests/HollerPTTTests Apps`.
- `swift test --filter HollerPTTTests` output buffers when piped; run it in the background with a poll loop (pattern in the session scratchpad) to avoid hangs masquerading as silence.
- macOS `/bin/bash` is 3.2; zsh aborts a `&&` chain on an unmatched glob — run shell self-tests under `/bin/bash`.
- The adversarial model-check harnesses (ZStrict*/ZProbe* Swift Testing files, ~1M runs this session) live only in the session scratchpad `review-scratch*/` directories, not in the repo. Candidate for an opt-in stress suite later; not needed now.

## Records

- Dollhouse memory `holler-project-context`: entries tagged `2026-08-22` (state, stop point, round-21 finding).
- Claude auto-memory `project_holler.md` mirrors the same state.
- Previous handoff: `HANDOFF_2026-08-21.md`.
