# Holler — handoff 2026-08-22

Read `CLAUDE.md` and `CONTRIBUTING.md` first (binding; both changed today in #42). Activate the Dollhouse ensemble `apple-product-team` and the memory `holler-project-context` (dated entries for this session).

## Direction from Mick (2026-08-22, binding)

- **Minimal project that does what the spec says, on real devices.** No more hardening of the PushToTalk gate beyond what device testing shows is needed.
- **PRs are a file or two; three or four when wiring is unavoidable; never tens of files.** A PR that grows past that is split, not merged. (#33 reached 31 files / 24 commits; it was split into #43, #50, #45, #46, #47, #48.)
- **One review pass per change**, recorded in the PR; then commit and stop. No review→fix→re-review loop. A rebase with an identical `git range-diff` keeps the review.
- **Codex threads get a decision** — fixed (which commit) / declined (why) / tracked (which issue) — and are resolved. No fix commit by default. Reason: the gate's 41 state variables came from a loop where every Codex interleaving got a new latch, which created the next interleaving.
- If a PR goes red after its one round, split it rather than patch it.

## Where things are (2026-08-22, end of session)

**main** = `88b436a`. The whole #33 split is merged; #33 is closed; the worktree `~/Developer/holler-wt2` and `feature/2-ios-shell` are gone.

Merged today, in order: **#24** health baseline (#13; squash `d31813f`, Codex leftovers → #40) · **#43** PushToTalkChannelControlling seam (closes #26) · **#50** platform-neutral gate + core/quick-tap suites (replaces #44; two P1 fixes: `beginRequested` set before the awaited begin command; `press()` guards `systemTransmitting`) · **#45 / #46 / #47** test-only gate suites · **#48** AppRoot restart teardown + `SessionServices.stop()`. HollerPTTTests: 176 tests on main.

Open: **#42** (the PR-size / one-pass / Codex-decision rules in CONTRIBUTING.md, CLAUDE.md, review-team README) and **#39** (this handoff) — both docs-only, rebased onto `88b436a`, merge when green.

Issues opened today: **#40** (health-script manifests / xcbeautify status), **#41** (collapse the gate's 41-variable state model to an explicit state enum + one epoch, in small PRs — do this before any further gate work; the Codex test-strength notes from #45–#47 and the two quick-re-press P1s from #50 are all tracked there), **#49** (restart teardown: stop services without waiting on the PTT leave; `ConnectionSupervisor.stop()` must cancel its event pump; drive coordinator disconnection before detaching; keep the old gate alive past the bounded wait while cleanup is outstanding).

Known gaps remain in ADR-0005 and #34–#38. **#38** (membership invisible in the UI; no reconnect after a terminal leave) matters for device-test UX.

## Path to devices (in order)

1. **Signing + entitlements (#11).** PushToTalk does not run in the simulator. Needs the Apple Developer team in `project.yml`, bundle IDs, `com.apple.developer.push-to-talk`, background modes `push-to-talk` + `audio`; then a TestFlight or direct install on two iPhones. This is the actual blocker. (The APNs provider key is *not* needed here — it belongs to #6.)
2. **Deploy the relay.** The app's relay URL is a placeholder; `relay/` was never deployed (Cloudflare — needs Mick's auth; ask before deploying). A baked URL is fine for the first test; settings (#10) are not required.
3. **APNs push restore (#6)** for background/killed-app receive: relay-side provider key + the app's ephemeral token path. Skippable for a first foreground two-phone test; required for "always-on".

Not needed before the first device test: #25 (HollerAudio tests), #10 (settings), #8 (call interruptions), Watch/Mac shells, #41, #49.

## Tooling facts

- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` for everything (`xcode-select` still points at CLT). `scripts/verify.sh all` and `scripts/verify.sh sim Holler-iOS` both pass locally; the sim lane is the only thing that compiles `#if os(iOS)` files.
- SwiftLint strict: file ≤200 lines, type body ≤150, line ≤120. `PushToTalkGatedControl+Speaker.swift` is at exactly 200.
- GitHub review-thread queries must **paginate** (`--paginate`, `first:100`): #24 had 109 threads; a `first:50` query hid unresolved ones.
- `gh pr merge --delete-branch` on a PR that other PRs are based on closes those PRs irrecoverably (cannot reopen once the base is gone). Retarget dependents first.
- GitHub may refuse a rebase merge ("This branch can't be rebased") for long branches with no visible cause; squash is the allowed fallback (ruleset permits squash + rebase).
- macOS `/bin/bash` is 3.2; BSD `sed` has no `\b`.

## Records

- Dollhouse memory `holler-project-context`: entries tagged `2026-08-22`.
- Claude auto-memory `project_holler.md` + `feedback_small-prs-no-review-loop.md`.
- Previous handoff: `HANDOFF_2026-08-21.md`.
